streamlit
pandas
anthropic>=0.40.0
# DeepSeek is OpenAI-compatible — reached via the OpenAI SDK + base_url override
openai>=1.40.0
# httpx is imported directly in server.py / jerry_gpt.py to set streaming timeouts.
# It ships as a transitive dep of anthropic/openai, but a direct import must be
# declared explicitly — otherwise a resolver change drops it and Jerry 500s with
# "No module named 'httpx'".
httpx>=0.27
extra-streamlit-components>=0.1.70
gspread>=6.0.0
google-auth>=2.20.0
psycopg2-binary>=2.9.9
# File I/O for Jerry GPT — uploads (Office extraction) + document generation
python-docx>=1.1.0
python-pptx>=1.0.0
openpyxl>=3.1.0
reportlab>=4.0.0
# Plain-HTML rebuild (server.py) - FastAPI replaces Streamlit when APP_MODE=html
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
