# Streamlit secrets — never commit
.streamlit/secrets.toml

# Python
__pycache__/
*.pyc
.venv/
venv/

# Editor / OS
.DS_Store
.vscode/
.idea/

# Environment files
.env
.env.local

# Roadmap scraper — local debug dumps + scraper venv. The distilled
# markdown in jerry_gpt_knowledge/ IS committed; raw dumps are not.
_scrape_dump/
.scrape_venv/
scrape_log.txt

# Generated static build (build_static.py) - regenerate, do not commit
site/

# Override-credential store written at runtime (hashes, but not ours to publish)
customized_login_store.json
